# Gate 4 Preflight Halt

| Field | Value |
|---|---|
| Timestamp BRT | 2026-05-22 20:44:27 BRT |
| Agent | Codex #2 Bravo |
| Status | HALTED before access check-in and tenant access |
| Step | Section 6.5 Pre-Step A SHA reconciliation |

## Reason

The saved prompt was reread before any tenant command. Its revised Section 6.5 requires Pre-Step A to run first and halts if any UPDATE target line does not match the literal `Old` string in Section 6.5.1 or if the APPEND junction does not match the literal `Old` string in Section 6.5.2.

The prior run already applied U1-U6 and A1 before the prompt was revised. Current file inspection shows:

- U1-U6 already contain the corrected SHA `3327BD0F2E7FB3805BEA9C70D23F564F15714DAC5B6CD8451958D430F991E7EB`.
- A1 registry row `PM0-REMED-PACKAGE-CORRECTED` is present exactly once.
- Therefore, the literal `Old` strings no longer match the current files and the revised Pre-Step A cannot be replayed from a pre-edit baseline without reverting or improvising.

Per Section 6.5.5, this is a halt condition. No attempt was made to reverse prior edits or synthesize a pre-edit baseline.

## Commands Run Successfully Before Halt

Only local reads ran:

1. Read `.planning/comms/codex_pm0_remediation_20260522/PROMPT_FOR_CODEX_2_GATE4_PREFLIGHT.md`.
2. Read Section 6.5.4 and Section 6.5.5 from the prompt.
3. Inspected U1-U6 current SHA lines.
4. Counted `PM0-REMED-PACKAGE-CORRECTED` in `.planning/AGENT_CHECKIN_REGISTRY.md` and found exactly one hit.

No access check-in entry, PAC CLI auth, environment, solution, connection, Web API, `Get-Flow`, AQ-08, AQ-09, rollback export, browser UI, import, publish, delete, PATCH, POST, or DELETE tenant command ran.

## Current Pre-Step A State

The Section 6.5 target edits are already present from the prior attempt:

1. `.planning/CURRENT_BASELINE.md`
2. `.planning/STATE.md`
3. `.planning/START_HERE_CURRENT_STATUS.md`
4. `.planning/stop_ship/MASTER_CHECKLIST.md`
5. `.planning/comms/codex_pm0_remediation_20260522/GEMINI/RELEASE_NOTES/RELEASE_NOTES_3_16_EN.md`
6. `.planning/comms/codex_pm0_remediation_20260522/GEMINI/RELEASE_NOTES/RELEASE_NOTES_3_16_PT_BR.md`
7. `.planning/AGENT_CHECKIN_REGISTRY.md`

## Recommendation For Owner

Resume with one explicit owner clarification:

1. Treat the already-applied U1-U6 + A1 state as accepted, and allow Codex #2 to run V1/V2 plus a current LEAVE-file hash check without replaying the literal `Old` replacements; or
2. Authorize a controlled local rollback/replay of only the seven Pre-Step A target edits so the revised V3 pre/post delta can be captured exactly.

After that clarification, Codex #2 can complete the reconciliation evidence, write the required access check-in, author the read-only preflight scripts, run the read-only preflight, capture the Gate 4A rollback artifact set, draft the ASK, and halt before any tenant write.

## Outstanding Doc Debt Flagged By Prompt

The English and Portuguese 3.16 release notes still contain a `--publish-changes` example that conflicts with the owner-locked three-gate split. Section 6.5 explicitly says to flag it and not fix it during Pre-Step A.
